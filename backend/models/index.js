const UserModel = require('./User.Model')
const EmailVerified = require('./Email.Model')
const HotelModel = require('./Hotel.Model')
const RoomModel = require('./Room.Model')
const ReservationModel = require('./Reservation.Model')

module.exports = {
  UserModel,
  EmailVerified,
  HotelModel,
  RoomModel,
  ReservationModel
}
